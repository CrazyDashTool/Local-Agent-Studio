# ComfyUI Workflow Pack

Draft plugin for Local Agent Studio.

## What it adds

- Import custom workflow JSON
- Register image/video models
- Validate workflow requirements

Put workflow files beside `plugin.json` in a real plugin repository.
